{
  "framework": "nextjs",
  "buildCommand": "npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "outputDirectory": ".next",
  "env": {
    "NEXT_PUBLIC_API_URL": {
      "description": "Base URL of the deployed backend (e.g. https://aerointel-backend.up.railway.app). No trailing slash.",
      "required": true
    },
    "NEXT_PUBLIC_WS_URL": {
      "description": "WebSocket URL of the deployed backend (e.g. wss://aerointel-backend.up.railway.app). No trailing slash.",
      "required": true
    }
  }
}
